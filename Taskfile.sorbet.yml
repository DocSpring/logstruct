version: '3'

tasks:
  typecheck:
    desc: Run Sorbet type checking
    cmds:
      - scripts/typecheck.sh

  typecheck:rbs:
    desc: Run Sorbet type checking with experimental RBS signatures
    cmds:
      - bundle exec srb tc --enable-experimental-rbs-signatures
