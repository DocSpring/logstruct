/* eslint-disable @typescript-eslint/no-explicit-any */
// Auto-generated by scripts/generate_structs.rb

export * from './enums'
export * from '../../lib/log-generation/sample-data'

<% roots.each do |sn| -%>
export * as <%= sn.split('_').map(&:capitalize).join %> from './sources/<%= sn %>'
<% end -%>

export enum LogType {
<% roots.each do |sn| name = sn.split('_').map(&:capitalize).join -%>
  <%= name.upcase %> = "<%= name %>",
<% end -%>
}

export const AllLogTypes: Array<LogType> = [
<% roots.each do |sn| name = sn.split('_').map(&:capitalize).join -%>
  LogType.<%= name.upcase %>,
<% end -%>
]

